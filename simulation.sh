#! /bin/sh

echo `date '+%y/%m/%d %H:%M:%S'`
#################################################################
day=20181105
#for d in 10 50 150 100 200; do
for d in 10; do
#for f in 10 20 30 40 50; do
for f in 10; do
#################################################################
./waf --run "scratch/tcp --nFlows=${f} --coredelay=${d} " > tcp-f$f-d$d.dat
#################################################################

echo `date '+%y/%m/%d %H:%M:%S'`
done
done
